<template>
  <div>测试页面</div>
  <!-- <div ref="childRef" class="childRef"></div> -->
</template>
<script setup>
import { ref } from "vue";
// import { renderAsync } from "docx-preview";
// const childRef = ref(null);
// const docxUrl = new URL("@/assets/test.docx", import.meta.url).href;
// console.log("docxUrl:", docxUrl);
// fetch(docxUrl)
//   .then((response) => response.arrayBuffer())
//   .then((docxBuffer) => {
//     renderAsync(docxBuffer, childRef.value, null, {
//       className: "custom-docx", // 自定义CSS类名前缀
//       inWrapper: true, // 是否使用包装容器
//       ignoreWidth: false, // 是否忽略页面宽度
//       ignoreHeight: false, // 是否忽略页面高度
//       breakPages: true, // 是否分页显示
//       renderHeaders: true, // 是否显示页眉
//       renderFooters: true, // 是否显示页脚
//       renderFootnotes: true, // 是否显示脚注
//       renderEndnotes: true, // 是否显示尾注
//       renderComments: true, // 是否显示评论
//       useBase64URL: true, // 使用Base64还是ObjectURL处理资源
//     })
//       .then(() => {
//         console.log("DOCX rendered successfully");
//       })
//       .catch((error) => {
//         console.error("Error rendering DOCX:", error);
//       });
//   });
</script>
<style></style>
